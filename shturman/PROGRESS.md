# PROGRESS — «Штурман»

> Если сессия оборвалась: прочитай этот файл сверху вниз, потом `DECISIONS.md`,
> потом продолжай с раздела «Дальше». Код — в `shturman/`, ветка —
> `claude/shtorman-mentor-panel-p6wc5w`.

## Статус блоков

| Блок | Состояние |
| --- | --- |
| 0. Разведка | ✅ готово (итоги в DECISIONS.md, Д-1…Д-6) |
| 1. Сервер-наблюдатель | ⬜ не начат |
| 2. Лента «Что происходит» | ⬜ не начат |
| 3. Карта проекта | ⬜ не начат |
| 4. Git для новичка | ⬜ не начат |
| 5. Пульс и сигналы | ⬜ не начат |
| 6. Онбординг и устойчивость | ⬜ не начат |
| Тесты ≥ 30 | ⬜ не начаты |
| README / CHANGELOG | ⬜ не начаты |

## Сделано

- Разведка среды: формат транскриптов (`~/.claude/projects/<munged>/*.jsonl`),
  типы записей, поля usage; хуки есть, но не используем; git/node на месте.
- Каркас каталогов `shturman/{lib,public,tests/fixtures}`.
- `DECISIONS.md` с решениями Д-1…Д-12.

## В работе

- Блок 1: модули `lib/` (вотчер, git, транскрипт, humanize, SSE) + `server.js`.

## Дальше (по порядку)

1. `lib/`: debounce → paths/ignore → watcher → git-парсеры → transcript-парсер
   → humanize → glossary → filedoc → tree → sse → pulse.
2. `server.js`: HTTP + маршруты `/`, `/events`, `/api/*`.
3. `public/`: index.html + style.css + app.js (лента, карта, git, пульс,
   словарь, тур, звук/уведомления).
4. Тесты на фикстурах (`tests/fixtures/*.jsonl` — обезличенные фрагменты
   реального транскрипта этой сессии).
5. README, CHANGELOG, самопроверка Definition of Done по пунктам 1–8.

## Известные баги / риски

- (пока пусто)

## Как проверить текущее состояние

```bash
cd shturman
node --test          # тесты (когда появятся)
node server.js       # панель на http://127.0.0.1:4517
```
