# Аудит перед вторым сезоном

Этап 0 задания 2: проверить, что первый сезон закрыт целиком, починить всё
сломанное и недоделанное, и только потом браться за новое.

---

## 1. Состояние на входе: Definition of Done первого сезона

Проверено полностью, до единого пункта.

| пункт DoD 1.0 | проверка | итог |
| --- | --- | --- |
| Тесты зелёные, их ≥ 25 | `npm test` пять раз подряд | **104 из 104**, ни одного плавающего |
| Прогоны 7 / 42 / 2026 без ошибок, различаются | `node sim/run.js --all` | 7 живых фракций из 12 на сидах 7 и 42, 6 на 2026; порядки вымираний разные |
| Каждый прогон воспроизводим байт-в-байт | повторный прогон + `md5sum -c` | все 9 файлов `results/` и `viz/replay.js` совпали |
| `arena.html` открывается с диска, играет все три прогона | headless-прогон всех трёх реплеев до конца | доиграл до поколения 29/29 каждый, экран победы показан, ноль ошибок в консоли |
| Схема `ARENA_DATA` проверена тестом, рендерер не читает отсутствующих полей | `tests/viz-contract.test.js` | сканер ловит 61 обращение, чувствительность подтверждена мутацией |
| `REPORT.md` на реальных числах | сверка с `results/` | сходится |
| Нет TODO и мёртвых веток | тест-сторож | чисто |

**Вывод: первый сезон закрыт, ремонтировать по DoD нечего.** Всё, что ниже, —
это то, что аудит нашёл сверх формальных требований.

---

## 2. Что нашёл и починил

### 2.1. Полсотни принудительных пересчётов стиля на кадр

`getComputedStyle(document.body).fontFamily` стоял прямо в коде отрисовки —
в **одиннадцати** местах, причём часть внутри циклов: по одному вызову на
каждую фракцию в полосе населения (12), по два на каждую дорожку (до 14) и до
трёх на каждую сшибку (до 21). Итого около полусотни обращений за кадр.

Каждое такое обращение заставляет браузер пересчитать стили документа. Значение
при этом не меняется в течение сессии — читать его повторно незачем вовсе.

Починено: шрифт читается один раз при старте в `UI_FONT`.

| замер (медиана по 30 поколениям) | до | после |
| --- | --- | --- |
| `drawField` | 5.70 мс | **3.60 мс** |
| `drawField`, худший кадр | 119.90 мс | **9.50 мс** |
| `renderAll` целиком | 5.60 мс | **4.30 мс** |

Худший кадр важнее медианы: 119 мс — это видимый рывок при первой отрисовке.

### 2.2. Вкладка жгла процессор на неподвижной картинке

В цикле анимации на паузе безусловно вызывался `drawField()` — «чтобы жила
река». Но рябь реки рисуется от `state.gen`, а он на паузе не меняется: кадр
выходил попиксельно тот же самый. Шестьдесят одинаковых кадров в секунду,
по 3.6 мс каждый — около четверти ядра на неподвижном изображении.

Починено: после остановки рисуется один финальный кадр (чтобы осело положение
фронта), дальше перерисовка только по событию — перемотка, смена вкладки,
изменение размера окна.

| перерисовок поля за 2 секунды | до | после |
| --- | --- | --- |
| на паузе | ~120 | **0** |
| при воспроизведении | ~110 | ~110 |

### 2.3. Не было замера производительности

`node sim/bench.js` не существовал — требование задания 2, но и без него
оптимизировать вслепую нельзя. Написан: меряет матч, круговой турнир, полный
прогон, пространственный режим, сборку реплея, а также свип и генетику, если
они уже собраны. Берётся медиана после разогрева JIT — среднее врёт на первом
запуске, минимум прячет типичный случай.

Базовые числа лиги из 12 стратегий (node v22, повторов 5):

```
матч 200 раундов                   0.1 мс   0.7 мкс/раунд
круговой турнир (78 матчей)       10.9 мс   140 мкс/матч
полный прогон, 30 поколений      321.4 мс
пространственный режим 20×20      36.6 мс
сборка ARENA_DATA                  4.9 мс
```

### 2.4. Профиль после роста лиги: одна стратегия из семнадцати

Прогноз «подорожает вдвое» не сбылся: после приёма претендентов и
«Безымянного» прогон стал дороже не вдвое, а в четырнадцать раз.

```
                              12 стратегий      17 стратегий
круговой турнир                10.9 мс           147.9 мс
полный прогон, 30 поколений   321.4 мс          4413.2 мс
цена одного матча              140 мкс           966 мкс
```

Рост числа пар с 78 до 153 объясняет ровно двукратную часть. Остальное —
цена одного матча, выросшая в семь раз, а она от размера лиги зависеть
не должна вовсе. Замер по стратегиям (200 раундов против Пацифиста,
медиана по 40 матчам) нашёл виновника сразу:

```
Резонанс          7.453 мс/матч   ← 77% всей стоимости
Интендант         0.305
Контрразведка     0.215
Соглядатай        0.201
...остальные      0.077 … 0.183
```

«Резонанс» на каждом раунде разворачивает свою модель противника в марковскую
цепь и прогоняет пять курсов на сорок шагов вперёд точным распространением
распределения по шестнадцати состояниям: около десяти миллионов операций
на матч.

**Оптимизировать не стал, и это осознанное решение.** Проверены два пути:

* *Запоминание оценки курсов.* Не работает: оценка зависит от всего вектора
  условных вероятностей, а он меняется каждый раунд — новое наблюдение
  сдвигает одну из шестнадцати позиций. Совпадений почти нет, кэш пустой.
* *Сокращение горизонта.* Работает и очень заметно — 40 → 20 шагов даёт
  8.14 → 3.73 мс, — но **меняет поведение**: результат расходится
  с исходным в 1 матче лиги из 17 при горизонте 20 и в 2 из 17 при горизонте 10.

Ускорять чужую стратегию ценой изменения её поведения нельзя: это уже не
оптимизация, а правка чужого замысла. А обстоятельство, которое делает
оптимизацию необязательной, простое — ничто из этого не тормозит:

```
полный прогон трёх сидов        13 с      разовая команда
весь набор тестов               50 с      147 тестов
стенд устойчивости              87 с      разовая аналитическая команда
отрисовка кадра                 4.3 мс    при бюджете 16.7 мс
```

Единственное, что стоило бы ускорить при дальнейшем росте лиги, — стенд
устойчивости: он один даёт 87 секунд из-за 24 миров, восемь из которых
играют матчи по 500 раундов. Профиль в руках, точка приложения известна.

---

## 3. Что улучшил мимоходом

### 3.1. Реестр разделён на ядро и принятых

Второй сезон обязан впустить в лигу претендентов и выведенного генетикой
«Безымянного». Прямая вставка в общий список сдвинула бы индексы, а индекс
входит в сид каждого матча — все прогоны первого сезона перестали бы
воспроизводиться, и числа в `REPORT.md` повисли бы в воздухе.

Реестр разделён: `core` — замороженная двенадцатка первого сезона, `admitted` —
принятые после него, `list = core.concat(admitted)`. Новички дописываются
только в хвост, поэтому сиды всех прежних пар остаются прежними.

### 3.2. Результаты первого сезона убраны в архив

`results/v1/` — девять прогонов двенадцатистратегийной лиги, на которые
опирается `REPORT.md`. Они остаются проверяемыми после того, как лига вырастет:
`node sim/run.js --league core` пересчитывает ровно их.

### 3.3. Найден риск, который выстрелил бы позже

У четырёх претендентов цвета оказались соседями цветов основной лиги:
Контрразведка `#7dd3fc` рядом с Зеркалом `#38bdf8`, Интендант `#fcd34d` рядом
с Прагматиком `#fbbf24`, Резонанс `#c4b5fd` рядом с Хаосом `#a78bfa`,
Ветер и Гора `#fda4af` рядом с Миротворцем `#f472b6`. В закрытом турнире из
пяти участников это незаметно, а в общей лиге из семнадцати четыре пары фракций
слились бы на карте территории и в полосе населения.

Тест на уникальность цвета проверял только основную лигу и это пропустил.
Решение — в разделе «Приём в лигу» файла `DECISIONS.md`: цвет считается
метаданными визуализатора, а не механикой, и при приёме назначается заново из
палитры, разведённой по тону. Проверка уникальности распространена на всю лигу
целиком.

---

## 4. Чего аудит НЕ нашёл

Отдельно стоит сказать, чего нет, чтобы не создавать ложного впечатления
тщательности там, где её не было.

- Ни одного `TODO`, `FIXME` или заглушки — это и так стерегущий тест.
- Ни одной несходящейся цифры в `REPORT.md`: выборочная сверка десятка
  утверждений с `results/` расхождений не дала.
- Ни одного плавающего теста: пять полных прогонов подряд дали 104/104.
  Единственный плавающий тест, который был в первом сезоне, — дуэли Интенданта
  на одном сиде — уже переписан на выборку из тридцати.
- Логических ошибок в движке не найдено. Проверялись подозрительные места:
  порядок операций в репликаторе, учёт матча с собственной копией, вывод сида
  матча, обработка вымерших фракций при раскладке дорожек.

---

## 5. Замер после стретч-целей

Три новых режима — поединок, рейтинг, летопись — на скорость арены не повлияли:
поединок работает в браузере и играет по раунду за клик, летопись это текст,
а рейтинг переигрывает матчи сам и в общий прогон не входит.

Контрольный замер (`node sim/bench.js --repeat 3`, лига из 17, node v22):

```
матч 200 раундов                   0.1 мс
круговой турнир (153 матча)      156.3 мс
полный прогон, 30 поколений     4579.9 мс
пространственный режим 20×20     286.4 мс
сборка ARENA_DATA                 10.7 мс
свип устойчивости              86953.6 мс
генетика, 150 поколений         2449.0 мс
```

Расхождения с замером из §2.4 — в пределах разброса машины; ничего не
замедлилось. Добавились две разовые команды:

```
node sim/elo.js --seed 42       ~101 с   из них ~87 с — свип (есть --no-sweep)
node sim/bundle.js                <0.1 с
```

Рейтинг — самая дорогая команда проекта после свипа, и по той же причине:
он переигрывает те же 24 мира, восемь из которых играют матчи по 500 раундов.
Точка приложения та же, что была названа раньше, и она по-прежнему не мешает:
это разовая аналитическая команда, а не часть рабочего цикла.

Отрисовка кадра после добавления вкладки поединка и зала славы не изменилась:
обе панели рисуются HTML, а не на холсте, и в кадровый бюджет не входят —
`drawFame` вызывается один раз при открытии вкладки «Аналитика».
