# PERF.md — плавность

Главный документ задания «чинить тормоза». Здесь базовая линия, бюджеты,
список виновных и таблица «исправление → до → после». Ни одна цифра тут не
взята с потолка: каждая воспроизводится командой.

```bash
npm run bench          # полный прогон стенда: 5000 файлов, 10 000 событий
npm run bench:quick    # облегчённый: 1000 файлов, 2000 событий
node bench/profile.js  # атрибуция: кто именно съедает главный поток
```

---

## Стенд

`bench/` — синтетическая нагрузка, заведомо тяжелее настоящей работы.

| Что | Сколько |
| --- | --- |
| файлов в проекте | 5 001 (плюс 800 файлов в `node_modules`, `.git`, `dist`, `coverage` — их вотчер обязан игнорировать) |
| событий в транскрипте | 10 000 |
| размер транскрипта | 41.6 МБ |
| длинные диффы | каждое сотое событие — 4 000 строк |
| длинный вывод команды | каждое пятидесятое — 1 200 строк |
| большой файл | 900 КБ |
| поток при шторме | 20 событий в секунду |

Данные детерминированы (свой генератор псевдослучайных чисел с фиксированным
зерном): прогон «до» и прогон «после» получают ровно один и тот же стенд,
иначе сравнивать их нельзя. Всё живёт во временных каталогах — ни проект
пользователя, ни его `~/.claude` не трогаются.

---

## Базовая линия — 03.08.2026, до единой правки

Прогон `npm run bench`, Linux 6.18.5, Node 22.22.2, Chromium 1194.

### Запуск

| Показатель | Значение |
| --- | --- |
| сервер начал отвечать | 1 027 мс |
| память сервера после разбора истории | **290.9 МБ** (после сборки мусора — 97.7 МБ) |
| до первого экрана | 35 мс |
| до первой отрисованной ленты | **2 589 мс** |

### Разметка

| Показатель | Значение |
| --- | --- |
| узлов DOM всего | **36 352** |
| из них строк дерева файлов | **5 073** |
| из них карточек ленты | 24 |
| куча вкладки после загрузки | 13.3 МБ |

### Покой

| Показатель | Значение |
| --- | --- |
| кадров в секунду | 58.6 |
| самая долгая задача | 54 мс |
| процессор сервера без панели | 0.3 % |
| процессор сервера с панелью | 0.4 % |

### Шторм: 20 событий в секунду, 20 секунд

| Показатель | Значение |
| --- | --- |
| кадров в секунду | 56.2 |
| самая долгая задача | **140 мс** |
| задач дольше 50 мс | **11** |
| главный поток занят | 825 мс за 22.5 с |
| задержка «событие → экран», среднее | **324 мс** |
| задержка, 95-я доля | **588 мс** |
| задержка, максимум | 630 мс |
| процессор сервера | 1.5 % |

### Прокрутка ленты

| Показатель | Значение |
| --- | --- |
| кадров в секунду | **47.7** |
| самая долгая задача | 63 мс |
| карточек в разметке | 23 |

### Два часа потока (замер 120 с, пересчёт на час)

| Показатель | Значение |
| --- | --- |
| куча вкладки | 14.9 → 5.9 МБ (сборщик отработал) |
| рост в пересчёте на час | −270 МБ, то есть роста нет |
| память сервера | 97.7 МБ |
| карточек ленты в разметке | 23 |

---

## Бюджеты

Числа взяты из задания; там, где отклонился, — обоснование в
[`DECISIONS.md`](DECISIONS.md).

| Показатель | Бюджет | База | Влезаем? |
| --- | --- | --- | --- |
| первый экран | < 1 500 мс | 35 мс | ✅ |
| первая отрисованная лента | < 3 000 мс | 2 589 мс | ✅ впритык |
| самая долгая задача главного потока | ≤ 50 мс | 140 мс | ❌ |
| задержка события, 95-я доля | < 200 мс | 588 мс | ❌ |
| карточек ленты в разметке | < 300 | 23 | ✅ |
| **узлов DOM всего** | **< 6 000** | 36 352 | ❌ |
| процессор сервера в покое | < 2 % | 0.4 % | ✅ |
| рост кучи за час | < 50 МБ | роста нет | ✅ |
| кадров в секунду под штормом | ≥ 50 | 56.2 | ✅ |
| кадров в секунду при прокрутке | ≥ 50 | 47.7 | ❌ |

Бюджет «узлов DOM < 6 000» — мой, задание такого не задавало (оно ограничивало
только ленту). Добавлен потому, что замеры показали: главный источник тормозов
не лента, а дерево файлов, и без числа на общий объём разметки его нечем
удержать. Обоснование величины — в `DECISIONS.md`, решение 51.

**Нарушено четыре бюджета из десяти.**

---

## Кто виноват — по вкладу в тормоза

Профилирование, а не догадки: `node bench/profile.js` меряет каждую
подозреваемую операцию отдельно, на живой странице с полным объёмом данных.

| # | Причина | Цена | Как часто |
| --- | --- | --- | --- |
| 1 | **Дерево файлов рисуется целиком, все 5 000 строк** | **416 мс** на один клик по папке | на каждый клик по папке, на каждую перезагрузку дерева |
| 2 | **Тепловая подсветка обходит все строки дерева** | **30 мс** | раз в секунду **и** на каждое файловое событие |
| 3 | **Стартовый снимок — 3.3 МБ** (500 событий с полными диффами) | 361 мс на приём и разбор | на каждое открытие панели |
| 4 | **Транскрипт опрашивается раз в 600 мс** | +300 мс к задержке в среднем | постоянно |
| 5 | Полное состояние в каждом обновлении (`pulse` раз в 2 с, `git` раз в 4 с) | десятки КБ | постоянно |
| 6 | Фильтрация буфера событий на каждое событие | 0.2 мс | на каждое событие |
| 7 | Перерисовка видимого окна ленты | 0.4 мс | на каждое событие |
| 8 | Принудительная раскладка (`scrollHeight`) | < 0.05 мс | ×3 на каждое событие |

**Вывод, который опроверг первую догадку.** Ощущение «лента дёргается»
обмануло: лента уже виртуализирована и стоит доли миллисекунды (строки 6–8 —
это шум, трогать их бессмысленно). Захлёбывается **карта проекта**: тридцать
шесть тысяч узлов разметки, из которых пять тысяч — строки дерева, и обход
этих строк раз в секунду. Именно поэтому чинить надо строго сверху вниз, а не
там, где «на глаз кажется».

### Замечание о самой методике

Первая версия профилировщика дала неверный ответ, и это стоит записать.
Замер «перерисовка дерева» подменял `innerHTML`, а подмена `innerHTML`
**убивает обработчики событий**. Следующий замер — «клик по папке» — попадал
уже на мёртвые строки, клик ничего не делал, и профиль показывал 0.1 мс
вместо 416 мс. Разрушающие замеры теперь идут последними, и в коде об этом
написано прямо. Мораль: инструмент замера сам нуждается в проверке, иначе
он даёт цифру, которая выглядит убедительнее правды.

---

## Исправления: до → после

Строго сверху вниз по списку виновных. Каждая строка — отдельный замер.

| # | Исправление | Показатель | До | После |
| --- | --- | --- | --- | --- |
| 1 | Виртуализация дерева файлов | узлов DOM всего | 36 352 | **906** |
| 1 | — то же | строк дерева в разметке | 5 073 | **30** |
| 1 | — то же | клик по папке | 416 мс | **5 мс** |
| 2 | Подсветка обходит только отрисованное, красит раз в кадр | обход дерева | 30 мс | **0.1 мс** |
| 2 | — то же | обходов при 20 событиях в секунду | 21 в секунду | **1 в кадр** |
| 3 | `raw` не уходит в браузер, снимок обрезан до 200 событий | стартовый снимок | 3.3 МБ | **≈120 КБ** |
| 3 | — то же | до первой отрисованной ленты | 2 589 мс | **778 мс** |
| 4 | `fs.watch` на транскрипте вместо опроса раз в 600 мс | задержка, среднее | 324 мс | **32 мс** |
| 4 | — то же | задержка, 95-я доля | 588 мс | **40 мс** |
| 5 | Кэш git по отпечатку `.git` + склейка вызовов | запусков процесса в минуту в покое | 75 | **0** |
| 6 | Лента копит события и выливает кадром | самая долгая задача под штормом | 140 мс | **0 мс** |
| 6 | — то же | задач дольше 50 мс за 20 с | 11 | **0** |
| 6 | — то же | главный поток занят | 825 мс | **0 мс** |
| 7 | `isHidden` без разрезания пути | вклад в клик по папке | ~27 мс | **< 1 мс** |

### Итог по бюджетам

| Показатель | Бюджет | База | Стало | |
| --- | --- | --- | --- | --- |
| первый экран | < 1 500 мс | 35 мс | 212 мс | ✅ |
| первая отрисованная лента | < 3 000 мс | 2 589 мс | **778 мс** | ✅ |
| самая долгая задача | ≤ 50 мс | 140 мс | **0 мс** | ✅ |
| задержка события, 95-я доля | < 200 мс | 588 мс | **40 мс** | ✅ |
| карточек ленты в разметке | < 300 | 23 | 23 | ✅ |
| узлов DOM всего | < 6 000 | 36 352 | **906** | ✅ |
| процессор сервера в покое | < 2 % | 0.4 % | **0.3 %** | ✅ |
| рост кучи за час | < 50 МБ | роста нет | 15 МБ | ✅ |
| кадров под штормом | ≥ 50 | 56.2 | **59.7** | ✅ |
| кадров при прокрутке | ≥ 50 | 47.7 | **59.6** | ✅ |

**Было нарушено четыре бюджета из десяти, стало — ноль.**

### Подтверждающий прогон после всех правок, включая бонусы

Повторный `npm run bench` на том же стенде — сверка не по промежуточному
состоянию, а по финальному коду:

| Показатель | Бюджет | Значение |
| --- | --- | --- |
| первый экран | < 1 500 мс | 157 мс |
| первая отрисованная лента | < 3 000 мс | 877 мс |
| самая долгая задача | ≤ 50 мс | **0 мс** |
| задержка события, 95-я доля | < 200 мс | **39 мс** |
| карточек ленты в разметке | < 300 | 23 |
| процессор сервера в покое | < 2 % | **0.1 %** |
| рост кучи за час | < 50 МБ | 6 МБ |
| кадров под штормом | ≥ 50 | 59.8 |
| кадров при прокрутке | ≥ 50 | 58.1 |

**Все бюджеты выполнены.** Разброс между прогонами (778 против 877 мс на
первой ленте, 59.6 против 58.1 кадра) — это шум машины, а не изменение:
контейнер делит процессор с браузером и стендом. Поэтому бюджеты и заданы с
запасом, а не впритык к лучшему замеру.

Одно число выросло: процессор сервера под искусственным штормом 1.5 % → 4.2 %.
Это цена `fs.watch`: раньше двадцать событий в секунду склеивались в один
разбор раз в 600 мс, теперь каждое разбирается сразу. Обмен сознательный —
четыре процента одного ядра под нагрузкой, которой в живой работе не бывает,
против задержки в полсекунды, которая бывает всегда. В покое нагрузка,
наоборот, упала: 0.4 → 0.3 %.

---

## Живая проверка на настоящей сессии

Панель смотрела за той самой сессией Claude Code, в которой шла эта работа —
проект `/home/user/Demo`, уровень A, живой транскрипт.

| Показатель | Значение |
| --- | --- |
| до первого экрана | 35 мс |
| до первой отрисованной ленты | **294 мс** |
| узлов DOM | 1 025 (лента 24 карточки, дерево 30 строк) |
| куча вкладки | 3.8 МБ |
| кадров за 20 с наблюдения | **59.3** |
| длинных задач | **0** |
| главный поток занят | **0 мс** |
| прокрутка ленты | **60.2 кадра** |
| процессор сервера | 0.6 % |
| память сервера | 71.1 МБ |
| ошибок JS | 0 |

**Ощущения.** Разница видна там же, где раньше раздражала: клик по папке в
карте проекта был заметной паузой — стало мгновенно. Лента под потоком
перестала «спотыкаться» на прокрутке. Событие появляется практически в
момент, когда Клод его совершил: раньше между действием и строкой была
ощутимая полусекунда, теперь строка успевает раньше, чем взгляд переходит с
терминала на панель.

---

## Ревизия функций

Ускорение за счёт вырезанных возможностей — это обход задачи. Проверяю
построчно: строк «удалено» нет.

| Функция | Работала | Работает | Что изменилось |
| --- | --- | --- | --- |
| Лента событий | ✅ | ✅ | копит и выливает кадром; в разметке окно, как и раньше |
| Фильтры ленты (9 штук) | ✅ | ✅ | без изменений |
| Поиск по ленте | ✅ | ✅ | без изменений |
| Пауза ленты | ✅ | ✅ | без изменений |
| «Просто / Подробно» | ✅ | ✅ | подробности собираются из разобранных полей, как и раньше |
| Подробности события (дифф, вывод) | ✅ | ✅ | длинный вывод обрезается с пометкой; целое — по `/api/event` |
| Стоп-сигналы с откатом | ✅ | ✅ | без изменений |
| Подсветка терминов | ✅ | ✅ | без изменений |
| Шпаргалка в шапке | ✅ | ✅ | без изменений |
| Словарь, 62 термина | ✅ | ✅ | без изменений |
| Карта проекта | ✅ | ✅ | **виртуализирована**: видно то же самое, в разметке — окно |
| Поиск по файлам | ✅ | ✅ | без изменений |
| Сворачивание папок | ✅ | ✅ | клик 416 → 5 мс |
| Тепловая подсветка | ✅ | ✅ | обходит отрисованное; **выключается** в профилях, но не исчезает |
| Карточка файла | ✅ | ✅ | без изменений |
| Git: ветка, изменения, история | ✅ | ✅ | данные те же, считаются по отпечатку, а не по таймеру |
| Дифф файла | ✅ | ✅ | без изменений |
| Пульс и приборы сессии | ✅ | ✅ | без изменений |
| Граф «куда смотрит Клод» | ✅ | ✅ | без изменений |
| Прошлые сессии | ✅ | ✅ | без изменений |
| Дневник и итоги | ✅ | ✅ | без изменений |
| Сигнал «Клод остановился» | ✅ | ✅ | без изменений |
| Экран выбора проекта | ✅ | ✅ | без изменений |
| Доступ с телефона, QR | ✅ | ✅ | без изменений |
| Настройки (17 строк) | ✅ | ✅ | **+8 строк** раздела «Скорость» |
| Диагностика | — | ✅ | **новое** |
| Отчёт о тормозах, запись телеметрии | — | ✅ | **новое** |

**Удалено: ноль. Стало хуже: ноль.**

Единственное, что человек может недосчитаться, — полный вывод очень длинной
команды сразу в карточке: теперь он обрезается на 20 000 знаках с пометкой,
сколько скрыто. Панель и раньше резала его на этом же числе при показе;
разница в том, что теперь лишнее не едет по сети. Целое событие доступно по
`/api/event`.

---

## Дополнительные задачи (бонусы)

| # | Задача | Сделано | Замер |
| --- | --- | --- | --- |
| 1 | Прогрев: интерфейс сразу, данные следом со скелетонами | ✅ заставка уходит по готовности (600–1700 мс), лента показывает скелет вместо «Пока тихо» | первая лента на живой сессии 294 → **228 мс** |
| 2 | Компактный индекс сессий вместо перечитывания JSONL | ✅ `lib/session-index.js`, ключ «путь + время правки + размер» | было 40 файлов × 128 КБ на **каждый** запрос раз в минуту; стало 0 чтений, пока сессии не менялись |
| 3 | Плавные переходы через View Transitions с откатом | ✅ где браузер умеет — снимок до/после, где нет — прежняя анимация сдвига | 180 мс, гасится профилем «без анимаций» |
| 4 | Режим «Запись тормозов» | ✅ 30 секунд телеметрии по кнопке, результат — файл | в «Диагностике» |

### Что нашлось при живой проверке бонусов

Первая версия прогрева **уронила панель целиком**: правка приземлилась не в
то место файла, и `boot()` оказался выше объявления `reduceMotion` —
`ReferenceError` до единой отрисовки. Все 371 тест при этом остались
зелёными, потому что ни один не открывает страницу целиком: они проверяют
модули и исходники, а не живой запуск. Поймал живой прогон.

Это довод в пользу того, что живая проверка стоит в списке не для красоты.
Модульные тесты не заменяют одного открытия страницы.

---

## Как повторить замеры

```bash
npm run bench          # полный прогон, роняет сборку при нарушении бюджета
npm run bench:quick    # облегчённый: 1000 файлов, 2000 событий
node bench/profile.js  # атрибуция: цена каждой операции отдельно
npm test               # 376 проверок, из них 31 про производительность
```

Прогон стенда возвращает ненулевой код, если превышен любой бюджет: тормоза
ломают сборку, а не копятся незаметно. Проверено искусственной регрессией —
возврат отправки `raw` роняет три теста из двадцати шести.
