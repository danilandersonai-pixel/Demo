# Навыки агента, установленные через `npx skills`.
# Коммитим только skills-lock.json — по нему набор воспроизводится командой
# `npx skills update`. Само содержимое чужих навыков не вендорим: оно живёт
# в своих репозиториях под своими лицензиями.
.agents/
.claude/skills/
