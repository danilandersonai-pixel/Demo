# AUDIT.md — ревизия «Штурмана»

Два раздела: **этап 0** — проверка первой версии перед началом работы над
второй, и **ревизия функций** — таблица «функция → компьютер → телефон»,
которая гарантирует, что при переходе на мобильную оболочку ничего не
потерялось.

---

## Этап 0. Проверка первой версии

**Когда:** 31.07.2026, перед началом задания 2.
**Как:** `npm test` + прогон панели живым браузером (headless Chromium через
CDP, сценарий кликает по каждой функции и читает DOM). Скрипт аудита —
`tests/manual/audit-ui.js`, запускается против поднятого сервера.

### Автоматические тесты

```
# tests 198   # pass 198   # fail 0   # skipped 0
```

### Ручная проверка интерфейса — 29 из 30 пунктов зелёные

| Что проверялось | Результат |
| --- | --- |
| Тур первого запуска показывается и проходится | ✔ 5 шагов, закрывается |
| Лента наполняется событиями | ✔ 305 карточек |
| Фильтры ленты (8 штук + «Всё») | ✔ переключаются, счётчики меняются |
| Поиск по ленте фильтрует и сбрасывается | ✔ «npm» → 21 из 305 |
| Режим «Просто / Подробно» | ✔ 305 блоков сырых данных, снимается |
| Пауза ленты | ✔ включается и снимается |
| Клик по событию → карточка с деталями | ✔ разделы, факты, дифф |
| Карта проекта строится | ✔ 84 узла, 17 папок |
| Папки сворачиваются | ✔ 84 → 71 |
| Поиск по дереву | ✔ 44 файла на «.js» |
| Карточка файла с объяснением назначения | ✔ «Настройки Claude Code», дифф |
| Git-панель «Сейчас» | ✔ ветка, счётчик, список файлов |
| Таймлайн коммитов | ✔ 20 коммитов с человеческими подписями |
| Дифф коммита по клику | ✔ 48 строк |
| Словарь | ✔ 40 терминов, поиск сужает до 4 |
| Подсказка по значку «?» | ✔ всплывает с объяснением |
| Пульс сессии | ✔ файлы, команды, ошибки, время, токены |
| Статус Клода и уровень источника | ✔ «Клод работает», «уровень A» |
| Граф внимания + список | ✔ 14 узлов, 12 связей / 12 чипсов |
| Журнал прошлых сессий | ✔ сессия видна, активная помечена |
| Кнопка проверки сигнала | ✔ карточка появляется |
| Дайджест | ✔ 17 304 символа markdown |
| Экран настроек | ✔ 5 пунктов |
| Ошибки JS на странице | ✔ ни одной |
| Телефон 360px: горизонтальная прокрутка | ✔ отсутствует |
| **Телефон 360px: зоны касания ≥ 44px** | **✖ 27 кнопок из 37 мельче** |

### Что найдено и что с этим сделано

1. **Зоны касания на телефоне меньше 44px** (27 кнопок из 37). Это не поломка
   первой версии — мобильных требований в задании 1 не было, — но прямое
   требование блока 3 задания 2. **Закрыто в блоке 3.**

2. **Ложная тревога аудита: «тур не показывается».** При первом прогоне
   сценарий упал на середине и не убил Chromium; второй прогон подключился к
   тому же живому экземпляру, где тур уже был пройден и записан в
   `localStorage`. Продукт ни при чём — исправлен сам сценарий аудита
   (гарантированное завершение браузера). Зафиксировано, чтобы вывод
   «29/30» читался честно.

3. **Фильтры «поиск», «git», «помощник» показали 0 событий.** Проверено: в
   ленте на момент аудита действительно не было ни `Grep`/`Glob`, ни событий
   git, ни субагентов — кольцевой буфер вытеснил ранние. Логика фильтров
   покрыта тестами, дефекта нет.

### Definition of Done первой версии — перепроверен целиком

| # | Пункт | Состояние |
| --- | --- | --- |
| 1 | Панель поднимается в любой папке, всё офлайн | ✔ зависимостей нет |
| 2 | Своя сессия видна в ленте | ✔ 305 карточек живой сессии |
| 3 | Карта обновляется вживую, подсветка гаснет | ✔ тесты вотчера + живой прогон |
| 4 | Git совпадает с `git status`, словарь ≥ 25 | ✔ тест сверки, 40 терминов |
| 5 | Детектор остановки срабатывает | ✔ 13 тестов + сквозной прогон |
| 6 | Уровень B остаётся полезным | ✔ тест деградации |
| 7 | Тесты зелёные, документация в порядке | ✔ 198/198 |
| 8 | Проект не изменён, git только чтение, только 127.0.0.1 | ✔ три отдельных теста |

**Вывод: DoD 1.0 зелёный целиком.** Можно переходить к блокам задания 2.

---

## Ревизия функций: компьютер против телефона

Правило этого задания: ни одна функция первой версии не должна потеряться или
стать хуже. Проверено сценарием `tests/manual/audit-v2.js` — он прогоняет
панель дважды, на 1500px и на 360px, и кликает по каждой функции.

**Результат последнего прогона: 31 из 31 пункта зелёные, «потеряно» — ноль.**

### Лента и события

| Функция | На компьютере | На телефоне | Статус |
| --- | --- | --- | --- |
| Лента «Что происходит» | левая колонка во всю высоту | вкладка «Лента», она же стартовая | ✔ |
| Человеческие формулировки | без изменений | без изменений | ✔ |
| Фильтры по 9 типам | чипсы в две строки | те же чипсы, прокручиваются вбок | ✔ |
| Поиск по ленте | поле над фильтрами + клавиша `/` | то же поле, клавиатура с кнопкой «Поиск» | ✔ |
| «Просто / Подробно» | переключатель в шапке панели | там же + дубль в «Настройках» | ✔ |
| Пауза ленты | кнопка «⏸ Пауза» | «⏸» без подписи, полоса накопления снизу | ✔ |
| Детали события | боковая панель справа, 560px | нижняя шторка, смахивается вниз | ✔ |
| Дифф в деталях | зелёное/красное, перенос по словам | то же, **без горизонтальной прокрутки** | ✔ улучшено |
| Вывод команды и ошибки | моноширинный блок | то же, ширина по экрану | ✔ |
| Работа помощника-субагента | отдельный отступ и фильтр | то же | ✔ |
| Сырые данные события | в конце карточки | в конце карточки | ✔ |
| «Спроси Клода» (при `--ask`) | кнопка в карточке | кнопка в карточке | ✔ |

### Карта проекта

| Функция | На компьютере | На телефоне | Статус |
| --- | --- | --- | --- |
| Живое дерево файлов | правая колонка, верхняя панель | вкладка «Карта» во весь экран | ✔ |
| Тепловая подсветка | гаснет за 90 с | то же | ✔ |
| Сворачивание папок | клик по строке | строки высотой 44px | ✔ улучшено |
| Поиск по дереву | поле над деревом | то же | ✔ |
| Карточка файла с объяснением | боковая панель | нижняя шторка | ✔ |

### Git

| Функция | На компьютере | На телефоне | Статус |
| --- | --- | --- | --- |
| Ветка и её смысл | правая колонка, нижняя панель | вкладка «Git» | ✔ |
| Счётчик несохранённого | крупная цифра + пояснение | то же | ✔ |
| Список изменённых файлов | клик открывает дифф | строки 44px | ✔ улучшено |
| Таймлайн коммитов | вкладка «История» | та же вкладка | ✔ |
| Дифф коммита | боковая панель | нижняя шторка | ✔ |
| Словарь (40 терминов) | шторка справа | шторка снизу, через «⋯» | ✔ |
| Значки «?» у терминов | всплывающая подсказка | то же, зона касания 44px | ✔ улучшено |

### Пульс и сигналы

| Функция | На компьютере | На телефоне | Статус |
| --- | --- | --- | --- |
| Сводка (файлы, команды, ошибки, время) | третья колонка | вкладка «Пульс», сетка 2×2 | ✔ |
| Оценка токенов | строка под сводкой | то же | ✔ |
| Граф внимания | SVG на всю ширину колонки | SVG по ширине экрана | ✔ |
| Список внимания | переключатель «Граф/Список» | тот же переключатель | ✔ |
| Журнал прошлых сессий | внизу третьей колонки | внизу вкладки «Пульс» | ✔ |
| Открытие архивной сессии | лента + полоса «Вернуться» | то же, вкладка сама переключается на ленту | ✔ |
| **Детектор остановки** | карточка + звук + уведомление | **+ вибрация, бейдж, подсветка шапки** | ✔ улучшено |
| Статус Клода | чип в шапке | чип в липкой шапке, виден всегда | ✔ улучшено |

### Оболочка

| Функция | На компьютере | На телефоне | Статус |
| --- | --- | --- | --- |
| Тур первого запуска | 5 шагов, карточка сбоку | 5 шагов, карточка над/под подсветкой | ✔ |
| Переключатель проектов | чипсы в шапке | чипсы отдельной строкой | ✔ |
| Уровень источника (A/B) | чип в шапке | в «Настройках» → «О наблюдении» | ✔ |
| Дайджест «Итоги» | скачать / скопировать | **+ системное «Поделиться»** | ✔ улучшено |
| Настройки | шторка справа | шторка снизу, через «⋯» | ✔ |
| Тема оформления | кнопка + клавиша `t` | в «⋯» и в «Настройках» | ✔ новое |
| Экран «Подключение» | QR и список устройств | то же | ✔ новое |

### Что стало лучше по сравнению с первой версией

- Все зоны касания доведены до 44px — на телефоне ничего не промахивается.
- Дифф больше не требует горизонтальной прокрутки.
- Сигнал остановки добавил вибрацию, бейдж на иконке и подсветку шапки.
- Появилась светлая тема и переключатель.
- Лента виртуализируется — на телефоне тысяча событий не тормозит.
- Найден и исправлен баг, которого в первой версии не было видно:
  смена «Просто/Подробно» после прокрутки опустошала ленту.

### Чего сознательно нет на телефоне

Ничего не убрано. Две вещи переехали, но остались доступны:

1. **Чип уровня источника (A/B)** — на 360px он не помещался в шапку рядом со
   статусом Клода. Уровень виден в «Настройках» → «О наблюдении», а если
   Штурман переходит на уровень B, он всё равно говорит об этом в ленте.
2. **Пять кнопок шапки** (словарь, итоги, тема, тур, настройки) — за кнопкой
   «⋯». Список строится из тех же действий, поэтому разойтись они не могут.

---

## Ревизия редизайна (задание 3): было → стало

Правило то же и строже: **ни одна функция не должна пропасть или стать менее
доступной**. Проверено тем же сценарием `tests/manual/audit-v2.js`,
переведённым на новую разметку.

**Результат последнего прогона: 32 из 32 пунктов зелёные, «потеряно» — ноль,
«стало хуже» — ноль.**

```bash
node server.js --port 4517 --quiet --no-open &
node tests/manual/audit-v2.js     # ожидается 32/32
node tests/manual/design-probe.js # ожидается «нарушений нет»
```

### Состояние Клода

| Функция | Было | Стало | Компьютер | Телефон |
| --- | --- | --- | --- | --- |
| Показать состояние | серый чип 12px в углу шапки | **картушка**: диск 56px + слово 44px | полоса под шапкой во всю ширину | та же полоса — она же шапка |
| Отличить «работает» от «ждёт» | цвет точки 8px | обход дуги против дыхания кольца + цвет + заливка | ✔ видно с трёх метров | ✔ видно в темноте |
| Сообщить об остановке | карточка в правом нижнем углу | та же картушка загорается латунью | ✔ там, куда человек уже смотрит | ✔ без перекрытия ленты |
| Отпустить сигнал | кнопка «Понял» в карточке | кнопка «Понятно» в картушке | ✔ справа в полосе | ✔ второй строкой + нажатие по прибору |
| Уровень источника A/B | второй серый чип рядом | строка «Откуда Штурман берёт данные» | ✔ в настройках | ✔ в настройках |
| Заголовок вкладки при сигнале | «🔔 Клод ждёт» | «Клод ждёт вас — Штурман» | ✔ | ✔ |

### Лента

| Функция | Было | Стало | Компьютер | Телефон |
| --- | --- | --- | --- | --- |
| Фильтры (9 типов) | таблетки в три ряда, 150px высоты | одна полоса 44px с прокруткой | ✔ один ряд | ✔ один ряд, свайп вбок |
| Поиск | отдельная строка под шапкой панели | поле в шапке панели | ✔ рядом с заголовком | ✔ первая строка панели |
| «Просто / Подробно» | переключатель из двух кнопок | одна кнопка-выключатель | ✔ с подписью | ✔ значком |
| Пауза | кнопка «⏸ Пауза» | то же, значком на телефоне | ✔ | ✔ |
| Тип события | цветная полоска слева, 5 цветов | значок в кружке; цвет только у «сделано» и «сломалось» | ✔ | ✔ |
| Пустая лента | один текст на три причины | три разных экрана с кнопкой выхода | ✔ | ✔ |
| Виртуализация | высота строки формулой | высота меряется по факту | ✔ | ✔ |

### Карта, git, пульс

| Функция | Было | Стало | Компьютер | Телефон |
| --- | --- | --- | --- | --- |
| Дерево файлов | строки с эмодзи 📁📄 | те же строки со штриховыми значками | ✔ | ✔ 44px |
| Тепловой след | жёлтая заливка | латунная заливка той же механики | ✔ | ✔ |
| Ветка и «?» | «?» переносился на свою строку | значок в одной строке с плашкой | ✔ | ✔ |
| Счётчик несохранённого | цифра слева от абзаца | цифра над пояснением | ✔ | ✔ |
| Чистое дерево | «0» и 380px пустоты | экран «Всё сохранено» + «Открыть историю» | ✔ | ✔ |
| Плитки пульса | подписи капсом 11px | подписи предложением 15px | ✔ | ✔ |
| Шапка секции без содержимого | пустая полоса 48px | полоса убрана | — | ✔ 48px вернулись содержимому |

### Оболочка

| Функция | Было | Стало | Компьютер | Телефон |
| --- | --- | --- | --- | --- |
| Первый запуск | сразу тур с шага «Лента» | приветствие → тот же тур | ✔ | ✔ |
| Тур | 5 шагов | 5 шагов, первый — про картушку | ✔ | ✔ |
| Тема | авто / тёмная / светлая | то же + три пресета | ✔ | ✔ через «⋯» |
| Плотность | не было | «Просторно / Компактно», +38% событий на экран | ✔ новое | ✔ новое |
| Переключатель проектов | чипсы в шапке | чипсы в шапке | ✔ | ✔ раздел в «⋯» |
| Итоги, словарь, подключение | шторка | шторка | ✔ | ✔ |
| Витрина компонентов | не было | `/design.html` | ✔ новое | ✔ новое |

### Строк «потеряно» и «стало хуже» — ноль

Единственное, что переехало: **переключатель проектов на телефоне** — из
отдельной полосы под шапкой в раздел «Проект» списка «⋯». Причина — картушка
заняла место шапки. Путь стал на одно нажатие длиннее, но у большинства
проект один, и полоса тогда не показывалась вовсе; в списке «⋯» видно и
название, и путь, и какой проект открыт сейчас, — то есть **больше**, чем
показывали чипсы.

---

## Аудит перед заданием «запуск в один клик»

Первым делом — проверка того, что уже есть: тесты, живой прогон каждого блока
первой версии и отдельно трёх названных в задании (переводчик, словарь,
сигналы). Всё сломанное чинилось до начала новой работы.

### Автоматика

```
npm test                        → 298 passed, 0 failed
node tests/manual/audit-v2.js   → 32/32, строк «потеряно» нет
node tests/manual/design-probe.js → нарушений нет (контраст, кегли, касания, клавиатура)
node tests/manual/design-motion.js → 10/10 (движение, reduced-motion, отзывчивость)
node server.js --check          → все проверки зелёные, кроме «порт занят» (занят своим же сервером)
```

### Живой прогон блоков первой версии — 13 из 13

| Что проверялось | Результат |
| --- | --- |
| Переводчик: человеческие заголовки | ✅ 24 карточки, ни одна не начинается латиницей |
| Переводчик: «почему» под каждым событием | ✅ 24 пояснения на 24 карточки |
| Лента: девять фильтров | ✅ все кликаются; у «git» и «помощник» в буфере нет своих событий — это данные, а не поломка |
| Режим «Подробно» | ✅ 19 блоков исходных данных |
| Словарь: открывается и наполнен | ✅ |
| Словарь: поиск | ✅ по «коммит» — 4 статьи, по «ъъъ» — экран «Такого слова нет» с кнопкой «Показать все слова» |
| Значок «?» у термина | ✅ всплывает объяснение |
| Сигнал «Клод остановился» | ✅ картушка загорается латунью, заголовок вкладки меняется |
| Сигнал отпускается | ✅ кнопкой «Понятно» и нажатием по прибору |
| Ошибки видны отдельным фильтром | ✅ |
| Карта, git, пульс, сессии, граф | ✅ 129 узлов дерева, ветка, 91 файл, 1 сессия, 14 узлов графа |
| Дневник (итоги сессии) | ✅ 20 819 символов, кнопки «Скачать файлом» и «Скопировать» |
| Ошибки JS | ✅ консоль чистая |

### Что починено на этом этапе

1. **Словарь вырос с 40 до 62 терминов.** Задание требует шестидесяти;
   добавлены те, что новичок встречает в ленте: `checkout`, `revert`,
   `reset`, `stash`, `rebase`, `origin`, `HEAD`, «неотслеживаемый файл»,
   CI, линтер, сборка, деплой, API, фреймворк, библиотека, рефакторинг,
   баг, лог, кеш, промпт, помощник-субагент и **откат**. Планка в тесте
   поднята с 25 до 60, чтобы словарь не «усох» обратно.
2. **Ложная тревога в моём же сценарии аудита**: проверка поиска по словарю
   печатала в поле ленты, а не в поле шторки, и показывала провал там, где
   всё работает. Сценарий исправлен — это дефект проверки, а не панели.

### Чего в первой версии нет, и это новая работа, а не поломка

Задание описывает вещи, которых в панели не было ни в каком виде. Они не
«сломаны» — их предстоит построить, и они разложены по блокам ниже:

- установка и запуск кликом, ярлык, один экземпляр, выключение из панели;
- экран выбора проекта вместо ввода пути;
- стоп-сигнал с командой отката;
- вкладка «Дневник» на телефоне;
- автозапуск при входе в систему и пункт «Открыть Штурман здесь».

**Вывод: Definition of Done первой версии зелёный целиком, можно идти
дальше.**
