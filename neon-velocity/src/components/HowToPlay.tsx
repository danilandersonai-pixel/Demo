// STUB: контракт справки. Владелец (агент menus) заменяет тело полностью, сохраняя экспортируемые сигнатуры.
export interface HowToPlayProps {
  onClose(): void;
}

/** Правила, управление, препятствия, комбо. Рендерит свой <Overlay layer={40}> + <Panel>. */
export function HowToPlay(_props: HowToPlayProps) {
  return null;
}
