# DECISIONS.md — журнал решений

Задание требует принимать спорные решения самостоятельно и фиксировать их здесь.
Формат: номер, решение, обоснование.

## D-01. Проект живёт в подкаталоге `sun-tzu-arena/` существующего репозитория

Задание говорит «git init», но работа идёт в уже существующем репозитории
(danilandersonai-pixel/Demo) на выделенной ветке `claude/new-session-9nkp3d`.
Новый `git init` внутри чужого репозитория создал бы вложенный repo и сломал бы
пуш в назначенную ветку. Решение: весь проект — самостоятельный каталог
`sun-tzu-arena/` со структурой из задания (`engine/`, `strategies/`, `sim/`,
`viz/`, `tests/`, `results/`), история — осмысленные коммиты в общей ветке.

## D-02. ES-модули (`"type": "module"`)

Node ≥ 18 гарантирован, сборки нет. ESM чище CommonJS и одинаково работает с
`node:test`. `viz/replay.js` — исключение: это браузерный скрипт
(`window.ARENA_DATA = …`), он подключается через `<script src>` и модулем не
является.

## D-03. Детерминизм: mulberry32 + независимые потоки на матч

Один глобальный поток ГПСЧ сделал бы результат зависимым от порядка матчей.
Вместо этого сид каждого матча выводится хэш-комбинацией
`combineSeed(seed, поколение, i, j)`, а внутри матча три независимых потока:
шум, ГПСЧ игрока A, ГПСЧ игрока B. Так стратегия, потребляющая случайность,
не сдвигает поток шума, а перестановка порядка матчей ничего не меняет.
`Date.now()`/`Math.random()` в симуляции запрещены; в данных нет таймстампов —
поэтому файл результата воспроизводим байт-в-байт.

## D-04. Шум — «дрожащая рука», обе стороны видят фактические ходы

С вероятностью 5 % ход искажается независимо для каждого игрока. Искажённый ход
и есть сыгранный: его видят оба игрока, включая самого «дрогнувшего» (в массиве
собственных ходов лежит фактический ход). Стратегия при этом может помнить своё
намерение в замыкании и сравнивать с фактом — этим пользуется «Дипломат»
(контрит-механика). Это стандартная модель implementation noise из литературы
по повторяющейся дилемме.

## D-05. Турнир перезапускается каждое поколение

Матрицу выплат можно было бы посчитать один раз, но при шуме матчи стохастичны.
Прогон полного круга «каждый с каждым» в каждом поколении даёт (а) честную
дисперсию, как в реальных эволюционных симуляциях, (б) данные для визуализатора:
каждое поколение — волна со своими исходами матчей. Вымершие (доля 0) не играют.
Селф-матчи (i против i, два независимых экземпляра) играются — они нужны
репликатору: приспособленность i включает встречи с собственной фракцией.

## D-06. Репликатор — дискретный, с порогом вымирания после шага

`x_i' = x_i · f_i / f̄`, где `f_i = Σ_j x_j · M[i][j]`, `f̄ = Σ x_i f_i`.
Порог применяется к уже обновлённым, ещё не нормированным долям: правило
спецификации «доля < 0.5 % → вымирание» выполняется буквально на каждом
шаге, после чего вектор нормируется к сумме 1.

## D-07. Условие победы фиксируется, но симуляция доигрывает все поколения

«Доминация > 60 %» или «вымирание остальных» записываются в `outcome`
(с номером поколения, где условие впервые выполнено), но эволюция не
останавливается: полные 30 поколений нужны отчёту и визуализатору. Если за 30
поколений условие не достигнуто, победитель — лидер по доле
(condition = `plurality`).

## D-08. В реплей идут агрегаты матчей, а не поход-история

200 раундов × ~50 матчей × 30 поколений × 3 сида — это мегабайты. В
`results/*.json` и `replay.js` пишутся средний счёт за раунд и частота
кооперации каждой стороны (`sa/sb/ca/cb`, три знака после запятой). Этого
достаточно и для фронта битвы, и для «battle juice» (частоты событий +3/+3,
+5/0, +1/+1 восстанавливаются из частот кооперации).

## D-09. Именование файлов результатов при нестандартных параметрах

Спецификация: `results/<seed>.json`. Это имя получает только прогон с
параметрами по умолчанию; ЛЮБОЕ отклонение добавляет суффиксы
(`-noise<v>`, `-gen<v>`, `-rounds<v>`, например `42-noise0.json`), чтобы
нестандартный прогон не мог затереть эталонный результат. То же правило — у
`sim/spatial.js` (`-noise/-epochs/-rounds`). В `replay.js` попадают только
эталонные сиды 7/42/2026 с параметрами по умолчанию.

## D-10. Стратегия видит только историю фактических ходов текущего матча

Интерфейс: `create(rng) → { move(mine, theirs) }`. Движок передаёт копии
массивов (`slice()`), поэтому мутация истории стратегией ни на что не влияет.
Никакого доступа к чужому коду, идентичности оппонента или состоянию турнира.
Ход, отличный от `'C'`/`'D'`, — ошибка движка (fail fast), а не молчаливая
подмена.

## D-11. Порядок стратегий фиксирован в `strategies/index.js`

Индексы стратегий — часть формата данных (матчи ссылаются на индексы).
Порядок в реестре менять нельзя без пересчёта результатов; это отражено в
комментарии в самом файле.

## D-12. Округление в данных — детерминированное

Доли — 6 знаков, счёт/частоты — 3 знака (`Math.round(x·10^k)/10^k`).
Одинаковые вычисления дают одинаковые double → одинаковую сериализацию,
байт-в-байт воспроизводимость сохраняется. Сумма округлённых долей может
отличаться от 1 на ~1e-6 — тесты проверяют с допуском, внутреннее состояние
нормировано точно.

## D-13. Чемпион лиги и карантин претендентов

«Чемпион основной лиги» для закрытого турнира определён как лучшее среднее
финальной доли по трём эталонным сидам (а не победитель одного сида): один
сид — лотерея, среднее — репутация. Претенденты от субагентов проходят
карантин (`sim/challenge.js`): проверка интерфейса, уникальности id, пробные
матчи и тест детерминизма (два матча с одним сидом обязаны совпасть ход в
ход — ловит Math.random/Date и глобальное состояние); сбой — дисквалификация
с причиной в `results/challenge.json`, а не падение турнира. Осознанное
ограничение: `import` исполняет top-level код модуля до карантина; песочницы
без внешних зависимостей нет, поэтому файлы претендентов — доверенный код.

## D-14. Пространственный режим — отдельные файлы данных

Сетка 20×20 (тор, соседство фон Неймана, клетка перенимает стратегию лучшего
соседа, при равенстве остаётся своей). Результаты пишутся в
`results/spatial-<seed>.json` отдельно от канонических `results/<seed>.json`:
тест байт-в-байт воспроизводимости основного прогона не должен зависеть от
того, запускался ли пространственный. В `replay.js` территории попадают
секцией `ARENA_DATA.spatial`; визуализатор работает и без неё.

## D-15. Параметры генетической эволюции (задание 2.0)

Популяция 64, 150 поколений — по заданию. Остальное подобрано так:
- **Фитнес — коэволюция**: каждый геном инициирует 12 матчей по 60 раундов
  (шум 5 %) против сидированно выбранных сородичей. Полный круговой турнир
  (2016 пар × 150 поколений) в ~5 раз дороже и не меняет качественного
  результата; выборка из 12 даёт ~24 матча на геном (инициатор + вызываемый)
  — достаточно, чтобы шум фитнеса был меньше межгеномных различий.
- **Турнирный отбор k=3** — стандартный мягкий пресс: сильные размножаются,
  но разнообразие не выкашивается.
- **Кроссовер 0.9, равномерный** — гены таблицы почти независимы по смыслу,
  равномерное перемешивание честнее одноточечного.
- **Мутация 0.02/ген** (~0.4 флипа на геном из 21) — держит поиск живым, не
  разрушая найденное; **элита 2** страхует от потери чемпиона.
- Чемпион — элитный геном финального поколения (а не «лучший за историю»:
  ранние рекорды фитнеса — шум маленькой выборки).
- «Безымянный» перезаписывается только каноническим прогоном
  `--seed 42` с конфигом по умолчанию; иные сиды пишут только
  `results/evolve-<seed>.json`.

## D-16. Круг 2 претендентов — те же субагенты, только свои логи

Доработку делают те же четыре агента, что писали стратегии (у них сохранён
контекст замысла). Каждому передаётся ТОЛЬКО его собственная строка турнира:
свои матчи (счёт и частоты кооперации обеих сторон), место и динамика доли.
Код и логи соперников не раскрываются — изоляция круга 1 сохраняется.
Файл дорабатывается по месту (тот же id), в досье добавляется поле
`round2` — что изменено и почему.

## D-17. Свип: оси и альтернативная матрица «Золотая лихорадка»

Оси по заданию: шум 0/5/10/20 %, длина матча 50/200/500. Альтернативная
матрица — T=8, R=5, P=1, S=0: соблазн предательства резко дороже
(T−R = 3 против 2), но и мир богаче (R−P = 4 против 2); условия дилеммы
сохранены (T>R>P>S, 2R > T+S). Вопрос стенда: удерживается ли кооперация,
когда за удар в спину платят вдвое. Свип гоняется на финальной лиге 2.0.

## D-18. Канонические результаты переходят на лигу 2.0

После ввода выживших претендентов и «Безымянного» реестр
`strategies/index.js` расширяется, и `results/<seed>.json` + `replay.js`
перегенерируются под новую лигу. `REPORT.md` остаётся архивным отчётом лиги
1.0 (его числа воспроизводимы на коммите `a70098c`, о чём в нём оставлена
пометка); актуальная лига описывается в `REPORT2.md`. ARENA_DATA получает
`version: 2` (добавлены секции duel-инспектора, свипа и ELO).

## D-19. Браузерный порт лиги — сборка из тех же исходников с проверкой паритета

Инспектору дуэлей и режиму «Сыграй сам» нужны стратегии в браузере, а
ES-модули под `file://` запрещены. Вместо дублирования логики `sim/bundle.js`
срезает import/export с ТЕХ ЖЕ исходников движка и стратегий и заворачивает
их в IIFE (`viz/league.js`). Обязательный этап сборки — паритет: каждая
стратегия играет один и тот же сидированный матч в Node и в бандле
(`node:vm`), последовательности ходов обязаны совпасть. Инспектор поэтому
не хранит поход-историю в данных: любой матч пересимулируется в браузере
тем же сидом байт-в-байт.
